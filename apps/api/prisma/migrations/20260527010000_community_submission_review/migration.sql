ALTER TYPE "CommunityPostStatus" ADD VALUE IF NOT EXISTS 'PENDING';
ALTER TYPE "CommunityPostStatus" ADD VALUE IF NOT EXISTS 'REJECTED';

ALTER TABLE "CommunityPost"
  ADD COLUMN IF NOT EXISTS "content" TEXT NOT NULL DEFAULT '',
  ADD COLUMN IF NOT EXISTS "authorNickname" TEXT,
  ADD COLUMN IF NOT EXISTS "contact" TEXT,
  ADD COLUMN IF NOT EXISTS "source" TEXT NOT NULL DEFAULT 'admin',
  ADD COLUMN IF NOT EXISTS "viewCount" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "likedSessionIds" JSONB NOT NULL DEFAULT '[]';

UPDATE "CommunityPost"
SET "content" = "summary"
WHERE "content" = '';
